<Window x:Class="Lab_rab_4_1_Safin_R._R._23_02.View.WindowRole"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_rab_4_1_Safin_R._R._23_02.View"
        mc:Ignorable="d"
        Title="Должности" Height="350" Width="450">
    <StackPanel Background="#FFD8D8D8">
        <Label Margin="5" HorizontalAlignment="Center" FontSize="14" FontWeight="Bold">
            Список должностей сотрудников
        </Label>

        <ListView x:Name="lvRole" 
                  Background="#FFD3CDCD" 
                  ItemsSource="{Binding ListRole}" 
                  SelectedItem="{Binding SelectedRole}"
                  Margin="5" Height="200">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Код" Width="50" 
                                        DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Наименование должности" Width="250" 
                                        DisplayMemberBinding="{Binding NameRole}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="Добавить" Height="25" Width="100" Margin="5"
                    Command="{Binding AddRole}"/>
            <Button Content="Редактировать" Height="25" Width="100" Margin="5"
                    Command="{Binding EditRole}"/>
            <Button Content="Удалить" Height="25" Width="100" Margin="5"
                    Command="{Binding DeleteRole}"/>
        </StackPanel>
    </StackPanel>
</Window>